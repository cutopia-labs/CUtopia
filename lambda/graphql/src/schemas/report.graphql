type Mutation {
  report(input: ReportInput!): String! @auth @rateLimit(window: "1m", max: 10)
}

input ReportInput {
  cat: Int!
  "review id or course id"
  identifier: String
  types: [Int]!
  description: String! @constraint(maxLength: 10000)
}
