directive @auth(role: Role = USER) on FIELD_DEFINITION

enum Role {
  USER
  OWNER
}

directive @rateLimit(
  max: Int
  window: String
  message: String
  identityArgs: [String]
  arrayLengthField: String
) on FIELD_DEFINITION
