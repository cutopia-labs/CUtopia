type Query {
  ranking: RankTable
}

type RankTable {
  rankedCourses(filter: RankingFilter): [RankedCourse]
}

input RankingFilter {
  rankBy: String! # numReviews, grading, content, difficulty, teaching
}

type RankedCourse {
  courseId: String!
  course: Course!
  numReviews: Int
  overall: Float
  grading: Float
  content: Float
  difficulty: Float
  teaching: Float
}
