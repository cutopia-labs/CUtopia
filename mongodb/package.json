{
  "name": "mongodb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "yarn jest",
    "build": "rm -rf node_modules && rm -rf lib && yarn install && tsc && cp -RL node_modules/ lib/",
    "postdeploy": "yarn install --prod=false"
  },
  "exports": {
    ".": "./lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cutopia-types": "^1.0.6",
    "dotenv": "^10.0.0",
    "mongodb": "^4.1.1",
    "mongoose": "^6.0.3",
    "nanoid": "^3.1.23",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/dotenv": "^8.2.0",
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.5"
  }
}
